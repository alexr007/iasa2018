package iasa.lesson4;

import java.util.List;

public class mailTest {
    public static void main(String[] args) {

/*
        SMTP server= new SMTP("server","login","password");
        Sender sender = new Sender("alexr@gmail.com");
        Body body = new Body();
        List<String> receivers;
        receivers.forEach(r->
                server.send(new Email(sender, r, body))
        );

*/
//        s.send(new Email("from", "to", "body", "attach"));


    }
}
